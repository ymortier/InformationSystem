<!-- ===================================================================== -->
<!-- Custom targets.                                                       -->
<!-- Set customBuildCallbacks = build.acceleo in your build.properties.    -->
<!-- ===================================================================== -->
<project name="Build Acceleo Module" default="noDefault">    
    <!-- ================================================================= -->
    <!-- Default target                                                    -->
    <!-- ================================================================= -->
    <target name="noDefault">
        <echo message="This file must be called with explicit targets" />
    </target>

    <!-- ================================================================= -->
    <!-- This will be called automatically after the compilation of each   -->
    <!-- Bundle... in dependency order.                                    -->
    <!-- ================================================================= -->
    <target name="post.compile.@dot">
        <acceleoCompiler 
            sourceFolder="${target.folder}"
            outputFolder="${target.folder}"
            dependencies="${target.folder}/../../org.obeonetwork.dsl.cinematic;${target.folder}/../../org.obeonetwork.dsl.environment;${target.folder}/../../org.obeonetwork.dsl.entity;${target.folder}/../../org.obeonetwork.dsl.soa;${target.folder}/../../org.obeonetwork.dsl.environment.binding.dialect;${target.folder}/../../org.eclipse.sirius;${target.folder}/../../org.obeonetwork.dsl.overview;${target.folder}/../../org.obeonetwork.dsl.cinematic.design;${target.folder}/../../org.obeonetwork.dsl.cinematic.edit;${target.folder}/../../org.obeonetwork.dsl.environment.edit;"
            binaryResource="false"
            packagesToRegister="">
        </acceleoCompiler>
    </target>    
</project>
